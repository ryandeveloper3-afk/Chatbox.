{
  "name": "chatbox-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --no-audit --no-fund && npm run build",
    "start": "cd frontend && npm run start -- --hostname 0.0.0.0 --port ${PORT:-3000}"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.25",
    "tailwindcss": "^4.3.3"
  },
  "dependencies": {
    "next": "^16.2.12"
  }
}
